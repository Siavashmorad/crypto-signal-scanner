# Deployment secrets only. Never commit real values.
SIGNAL_SCANNER_USERNAME=Siavashmorad
SIGNAL_SCANNER_PASSWORD_HASH=generated_pbkdf2_sha256_hash

# Modes:
#   signal_only         -> no orders
#   paper               -> virtual positions AFTER your approve (recommended start)
#   live_with_approval  -> real Tabdeal MARKET order AFTER your approve
EXECUTION_MODE=paper

# Only required for live_with_approval (server secrets, never in APK)
TABDEAL_API_KEY=
TABDEAL_API_SECRET=

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
OPENAI_RESPONSES_URL=https://api.openai.com/v1/responses
